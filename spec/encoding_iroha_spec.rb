# frozen_string_literal: true

RSpec.describe EncodingIroha do
  it "has a version number" do
    expect(EncodingIroha::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
